{{- $altbuildername := include "docs-csc.altBuilderName" . -}}
{{- $origbuildername := include "docs-csc.origBuilderName" . -}}
kind: BuildConfig
apiVersion: build.openshift.io/v1
metadata:
  name: {{ $altbuildername }}
  labels:
{{ include "docs-csc.labels" . | indent 4 }}
{{ include "docs-csc.altName" . | list $ | include "docs-csc.componentLabels" | indent 4 }}
spec:
  triggers:
    - type: ImageChange
      imageChange:
        from:
          kind: ImageStreamTag
          name: {{ $origbuildername | list $ | include "docs-csc.latestImageName" | squote }}
  output:
    to:
      kind: ImageStreamTag
      name: {{ $altbuildername | list $ | include "docs-csc.latestImageName" | squote }}
  strategy:
    type: Docker
    dockerStrategy:
      dockerfilePath: translation/Containerfile.alternate.builder
      buildArgs:
        - name: builder_image
          value: {{ $origbuildername | list $ | include "docs-csc.fullImageName" | squote }}
{{- .Values.components.alternate.repo | default dict | include "docs-csc.repoOverride" | indent 8 }}
  source:
{{ include "docs-csc.gitSource" . | indent 4 }}