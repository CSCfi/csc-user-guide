---
sudo: false
dist: trusty
addons:
  apt:
    packages:
      - aspell
      - aspell-en
install:
script:
  # Checks all *.md files with aspell.
  # The number of spelling mistakes that are allowed.
  - bash tests/aspellcheck.bash 2500
