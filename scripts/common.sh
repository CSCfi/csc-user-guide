#!/bin/bash

# Scripts are run from the top level directory of the documentation so all paths
# are relative to that.
app_dir="docs/apps"
ignores="--exclude=docs/apps/index.md --exclude=docs/apps/alpha.md --exclude=docs/apps/by_system.md"
